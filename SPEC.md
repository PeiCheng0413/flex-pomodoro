# 彈性蕃茄鐘 規格

給 Pixel 9 Pro 使用的 Android 彈性蕃茄鐘。不上架，透過 GitHub Releases＋Obtainium 安裝與更新。

## 名詞

- **專注（Session）**：從按下開始到結束的一整段，包含多輪工作與休息。帶有一個名稱（可空白）與休息比例。
- **輪（Round）**：一段工作，加上它之後的休息。
- **休息額度**：工作時間 ÷ 比例，無條件進位到秒。
- **超時**：休息超過額度的部分，倒數顯示為負數。

## 計時規則

- 三種狀態：**工作中**（正計時）、**暫停**、**休息中**（倒數）。
- 休息額度＝該輪工作時間 ÷ 比例（預設 5），**無條件進位到秒**，無上下限。
- 暫停時工作計時停止；暫停時間不算工作、不增加休息額度，另外記錄。
- 暫停中可直接進入休息，額度以暫停前累積的工作時間計算。
- 休息中**不能暫停**；可隨時按「開始工作」提早回去。
- 休息超過額度後顯示負數。超時**只顯示，不影響下一輪**，但計入統計。
- 下一輪工作從 00:00 重新正計時。

## 專注的開始與結束

- 開始前有名稱欄位，可留空白直接開始。
- **模板**＝名稱＋休息比例。點模板只填入欄位，不會直接開始。長按模板可編輯或刪除。
- 新輸入的名稱不會自動變成模板。
- 名稱綁定整次專注；要換名稱就結束後重開。
- 結束只能在 App 直向畫面**長按**「結束」。
- **自動結束**：休息超時或暫停持續超過 60 分鐘（可設定）時自動結束，結束時間回推到超時／暫停開始的那一刻，並發通知告知本次工作總時長。

## 提醒與通知

- 工作中不提醒。
- 專注進行中顯示**常駐通知**（前景服務），鎖定畫面可見，支援 Live Updates 狀態列膠囊。計時由系統 Chronometer 顯示。
- 通知按鈕依狀態變化，通知上**沒有**「結束」：

| 狀態 | 通知文字 | 按鈕 |
|---|---|---|
| 工作中 | 工作中 23:15 | 暫停、休息 |
| 暫停 | 已暫停 23:15 | 繼續、休息 |
| 休息中 | 休息 04:32（倒數） | 開始工作 |
| 超時 | 超時 -02:10 | 開始工作 |

- 休息倒數到 0：通知＋震動（聲音跟隨手機鈴聲模式）。
- 超時後每 5 分鐘輕震一次。

## 橫向專注模式

- 純黑背景，只顯示計時數字。
- 數字顏色區分狀態（皆為壓暗的顏色）：暗白＝工作、暗灰＝暫停、暗綠＝休息、暗紅＝超時。
- 單擊：工作 ↔ 暫停。長按：進入休息／回到工作。
- 螢幕常亮；App 視窗亮度強制為設定值（預設最低），轉回直向恢復系統亮度。
- 數字每分鐘微幅移動位置，防止 OLED 烙印。

## 直向畫面

- 跟隨系統深淺色，Material You 桌布取色；狀態色固定與橫向一致。
- 由上到下：名稱、大計時數字、「第 N 輪 · 下次休息 X:XX」、依狀態變化的按鈕、需長按的「結束」。
- 底部導覽：計時／統計／設定。

## 統計

- 以**名稱**分組：去除前後空白後完全相同才算同一組。空白名稱歸為「未命名」。
- 輸入名稱時自動提示歷史上用過的名稱。
- 首頁為名稱列表：總工作時間、次數、總超時，依總工作時間排序。可切換本週／本月／全部（預設全部）。
- 點入名稱：每筆紀錄（日期、工作時長、輪數、超時、暫停）與時間軸色帶（白＝工作、灰＝暫停、綠＝休息、紅＝超時）。
- 可單筆改名、整組改名（同名即合併）、刪除單筆紀錄。

## 設定

- 預設休息比例（預設 5）
- 自動結束門檻（預設 60 分鐘）
- 橫向亮度（預設最低）
- 模板管理
- JSON 匯出／匯入

## 資料

- 完全離線，存在手機本機（Room）。不開 Android 自動備份。
- JSON 匯出／匯入。匯入採**合併**：紀錄以 UUID 去重、模板同名跳過；匯入前顯示「將新增 N 筆紀錄、M 個模板」並確認。
- 每筆專注保存當時使用的比例，改模板不影響舊紀錄。
- App 被系統終止或手機重開機時，以時間戳記恢復計時狀態。

## 技術與發布

- Kotlin＋Jetpack Compose＋Room，介面只有繁體中文。
- 本機：Homebrew 安裝 Java 17＋Android 命令列 SDK，無線偵錯安裝到手機。
- 公開 repo `PeiCheng0413/flex-pomodoro`。推 `v*` 標籤後 GitHub Actions 以固定金鑰簽署 APK 並發布 Release，手機用 Obtainium 追蹤更新。
